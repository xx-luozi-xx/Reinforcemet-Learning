
#pragma once

namespace RL{
const char* SAVING_PATH = "./save/";
const int GAME_ROUND = 200; // game rounds (training rounds)
const double LEARNING_RATE = 0.2;
const double EPSILON = 0.7;// if rand(0,1)<= epsilon then do random action

}

